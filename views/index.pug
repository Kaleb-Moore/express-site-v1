extends layout.pug

block content

  article.portfolio-intro
    h1 My Portfolio
    p This portfolio is to show all of the interesting things I have done on my journey to becoming a Full Stack JavaScript Developer!

  article.portfolio-project-thumbs
    .projects-box.box.grid
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src=`${project.image_urls[0]}`, alt=`Thumbnail ${project.project_name}`)
            h5= project.project_name

